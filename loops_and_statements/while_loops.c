#include <stdio.h>

/**
 * Testing out the while loops
 * main - beginning of code
 * basic example
 * return - always 0
 */

int main(void)
{
	int i = 0;

	while (i < 25)
	{
		printf("%i\n", i);
		i++;
	}
	return (0);
}
